package dev.sunbirdrc.registry.entities;

// Enum representing the types of attestation available in the system
public enum AttestationType {
    MANUAL, // Manual attestation requires human intervention to verify and attest the data
    AUTOMATED // Automated attestation uses algorithms and predefined rules to verify and attest the data
}