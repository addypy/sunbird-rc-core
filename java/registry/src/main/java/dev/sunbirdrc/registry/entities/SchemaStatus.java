package dev.sunbirdrc.registry.entities;

// Enum representing the status of a schema in the registry
public enum SchemaStatus {
    DRAFT,      // The schema is in draft status and not yet published
    PUBLISHED   // The schema is published and available for use
}