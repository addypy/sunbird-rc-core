package dev.sunbirdrc.registry.entities;

// Enum representing the different types of flows in the registry system
public enum FlowType {
    ATTESTATION, // Flow type for attestation process
    NONE,        // Flow type for no specific process
    FUNCTION     // Flow type for a specific function or operation
}