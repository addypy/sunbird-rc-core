package dev.sunbirdrc.registry.entities;

// Enum representing the attestation status of an entity in the registry
public enum AttestationStatus {
    DRAFT,      // The entity is in draft status, not yet attested
    PUBLISHED   // The entity is published and attested
}